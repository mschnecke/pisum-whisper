## 1. Settle the technique

- [ ] 1.1 Re-run the failure-injection probe on win-x64 and record which exception each technique produces. The probe is a ~40-line console project replicating `SettingsStore.Write`'s two lines verbatim — `File.WriteAllText(path + ".tmp", json)` then `File.Move(tmp, path, true)` — inside the same `catch (Exception e) when (e is IOException or UnauthorizedAccessException)`. It needs no reference to this repository and is rewritten rather than preserved; the macOS results it produced are in design D1. Verify: T1 (a directory at the destination) throws either `IOException` or `UnauthorizedAccessException` — both are caught by `Write`'s filter, so either answer confirms it. **If T1 does not throw on Windows, stop and take T3** (a directory at `FilePath + ".tmp"`), which needs the same one-line arrangement; design *Migration Plan*, step 1.

## 2. The five write-failure tests

- [ ] 2.1 Replace the `FileShare.None` arrangement in `SettingsEditorTests.ACommitThatCannotBeWritten_NotifiesAndLogsRatherThanThrowingUnobserved` with `File.Delete(Store.FilePath)` + `Directory.CreateDirectory(Store.FilePath)`, carrying the comment from design D1 — what it stands in for, and that the lock it replaces only failed on Windows. Verify: the test passes on macOS, where it fails today.
- [ ] 2.2 Do the same for the four `PresetsViewModelTests` write-failure tests — `Add_`, `Save_`, `Delete_` and `Activate_WhenTheWriteFails_*`. Verify: all four pass on macOS; `dotnet test tests/Pisum.Whisper.App.Tests` reports 39 passed, 0 failed, where it reports 34 / 5 today.
- [ ] 2.3 Confirm the tests still fail for the right reason rather than passing by accident. Verify: temporarily revert `SettingsEditor.Commit()`'s `catch (SettingsException)` and confirm all five fail again — a test that no longer forces a write failure would stay green through that, which is the failure mode this arrangement change could introduce.

## 3. The four order-dependent `ToastPresenterTests`, and the preset race

- [ ] 3.1 **Measure before writing anything.** Establish why `Dispatcher.FromThread(_uiThread)` returns null for a `Thread.CurrentThread` captured inside an `[AvaloniaFact]`, and what distinguishes the sessions where it does not: log the managed thread id at construction, at `Present`, and for `Dispatcher.UIThread`, in a test run alone and in one run beside siblings. Verify: the record names which thread owns the dispatcher and which one the two-argument constructor captures. **Nothing in 3.2 may be written first** — a fix guessed ahead of this would pass for the same wrong reason the tests currently do (design D1b).
- [ ] 3.2 Repair all four — `PresentReturnsBeforeTheWindowExists`, `PresentCompletesWhenCalledFromANonUiThread`, `ThreeStackAndAFourthClosesTheOldest` and `PresentAfterTheUiThreadHasADispatcherShows` — using 3.1's answer — most likely the existing `internal ToastPresenter(TimeSpan, ILogger, Thread uiThread)` constructor, naming the thread that actually owns the dispatcher rather than the one the test is constructed on. `ToastPresenter` itself is not changed. Verify: each of the four passes **run entirely alone** (`--filter-method '*ToastPresenterTests.<name>'`), which is the state that fails today for the three measured that way; and the whole class passes under D1c's starvation recipe, where `PresentCompletes…` currently fails 4 runs in 6.
- [ ] 3.3 Confirm the repaired tests still fail when they should. Verify: temporarily invert `Present`'s null check so the gate always drops, and confirm all four fail — a test that no longer exercises the gate would stay green through that, which is the failure mode 3.2 could introduce.
- [ ] 3.4 Make `SettingsStorePresetRaceTests.APresetIsDeletedWhileTheActivePromptIsBeingResolvedRepeatedly` wait for its reader before deleting: the reader signals its first iteration through a `TaskCompletionSource`, and the test awaits that — **bounded**, so a genuinely dead reader still fails rather than hanging — before entering the deletion loop. Keep the `reads.ShouldBeGreaterThan(0)` guard; it is the line that caught this and deleting it would license a vacuous pass (design D1c). `SettingsStore` is not touched: the reproduction showed `Should.NotThrowAsync(reader)` passing, so the clone-and-replace held and only the scheduling assumption was wrong. Verify: the test passes 6 runs of 6 under the starvation recipe — 14 busy loops on a 10-core machine — where it currently fails 1 in 6, and `reads` is greater than 0 in every one.

## 4. The rotation latency test

- [ ] 4.1 Add `TimingTests` to `tests/Pisum.Whisper.Core.Tests/`, beside `ManualTests.cs` and in its shape: an `internal static` class with `Enabled` reading `PISUM_WHISPER_RUN_TIMING`, and a summary saying what it gates and why a skip is preferred to a runner filter. Verify: it compiles at 0 warnings.
- [ ] 4.2 Apply `Skip` / `SkipUnless` / `SkipType` to `FileLoggingRotationTests.WritesDoNotStallTheCallingThreadWhenTheFileRolls` only, leaving the rest of the class — and its `DisableParallelization` collection — untouched. Verify: an ordinary run reports it skipped **with its reason in the output**, and `dotnet test tests/Pisum.Whisper.Core.Tests --filter-method '*WritesDoNotStallTheCallingThreadWhenTheFileRolls' -e PISUM_WHISPER_RUN_TIMING=1` runs it.

## 5. The full run, twice over

- [ ] 5.1 Run the CI command on macOS ten times at normal load **and six times under D1c's starvation recipe** (14 busy loops on a 10-core machine), recording the numbers. Verify: `dotnet test Pisum.Whisper.slnx --filter-not-trait Category=Manual` is green with **one** filter — 625 selected, 6 skipped (5 `WindowsAutostartTests` + 1 timing), 619 passed, 0 failed — on **all sixteen**. The starved runs matter more than the idle ones: they are where `PresentCompletes…` fails 4 in 6 and the preset race 1 in 6 today, and a CI runner is a starved machine by construction.
- [ ] 5.2 Run the same command on win-x64 and record the numbers. Verify: green, 625 selected, 1 skipped (the timing test; `WindowsAutostartTests` runs there), 624 passed, 0 failed. Note whether `FileLoggingRotationTests`' latency test would have failed there had it not been gated — design D2 keeps the gate on Windows evidence alone, and if it never fails there either the honest follow-up is removing the gate.
- [ ] 5.3 **Run every repaired test on its own**, all ten: the five write-failure tests, the four `ToastPresenterTests` and the preset race test. Verify: each passes in isolation on both platforms. This is the second half of the verification and the check that would have caught the `ToastPresenterTests` regression when the readiness gate was added — the full-suite run that ticked those boxes was green three times in four.

## 6. Documentation

- [ ] 6.1 Correct `CLAUDE.md`'s *The test stack* section: the counts (620 → 629, and the `223 / 393 / 4` class and test split recomputed), a third gate beside `ManualTests` and `WindowsOnly` — `TimingTests` and `PISUM_WHISPER_RUN_TIMING` — and the rule design D3 establishes, that a test which cannot run somewhere declares itself skipped rather than being filtered out by the runner invocation. Correct the paragraph calling the rotation flake "a known flake going into change 12's CI", which is no longer where it is handled and was never observed on macOS. And add what D1b found: **`PerTest` isolation does not isolate `Dispatcher.FromThread`**, so an `[AvaloniaFact]` asserting on it can pass for no reason but its neighbours — the paragraph claiming `PerTest` is kept because `PerAssembly` is unsafe for global state now needs the qualifier that `PerTest` is not sufficient either. Verify: the counts match a real run, and `git show --stat` confirms `CLAUDE.md` lands with the tests it describes.
- [x] 6.2 Add the row to `openspec/ROADMAP.md`'s *Off-sequence changes* table. **Done during planning** — a roadmap row is a planning artifact, not implementation. It records this as the first off-sequence change that blocks a numbered one, where the table previously had `report-startup-failures` blocking nothing, and carries the measured 2026-09-03 numbers and the `rename(2)` cause.

## 7. Amend `add-packaging-ci` — done during planning

Its planning artifacts were committed at `6ee1736` describing a CI filter this change deletes.
Amending another change's planning artifacts is planning rather than implementation, so all four
were made in this change's own planning pass rather than left for apply. They are listed because
the record of *what* was amended, and why, belongs with the change that caused it.

- [x] 7.1 Rewrote `add-packaging-ci`'s design D9 — the `--filter-not-method` exclusion is gone, the CI command carries one filter, and D9 now states the rule (a skip explains itself, a runner filter does not) plus the measured per-platform counts. Its *Risks* entry for the rotation flake was rewritten to name this change. Verified: no `--filter-not-method` remains in that change's artifacts except where D9 names it as the thing it rejected.
- [x] 7.2 Reworded `specs/packaging/spec.md`'s requirement *Continuous integration builds and tests both platforms* from enumerating what may be excluded to constraining how, and replaced its third scenario with one covering any test that cannot run on a platform. Verified: `openspec validate add-packaging-ci --strict` passes, and the requirement no longer contradicts D9.
- [x] 7.3 Corrected `add-packaging-ci`'s task 5.2, which claimed "615 tests, the four manual ones report skipped" — wrong on both counts. Verified: it now names 625 selected with the per-platform skip counts, and says it is blocked on this change.
- [x] 7.4 Recorded the dependency in `add-packaging-ci`'s `proposal.md` *Impact*. Verified: it says task group 5 also depends on this change, and `openspec validate add-packaging-ci --strict` still passes.

## Not in this change

- **A `file-logging` requirement that logging does not stall the calling thread.** Its spec has six requirements and none says this, so the 500 µs test guards an invariant written down nowhere. Recorded in design *Open Questions*; it is why the test is gated rather than deleted.
- **A new latency bound.** Choosing a number that a shared runner passes is how a bound stops meaning anything. `file-logging`'s to set.
- **Any change to `SettingsStore`.** Design D4 — the failures are in how a failure is simulated, not in the code being tested.
- **An audit of the remaining 610 tests for order dependence.** Thirty-eight full runs — six of them starved — plus an alone-run of every repaired test is the evidence, not a per-test review.
- **Any change to `ToastPresenter`'s readiness gate.** It is correct, it fixed a real startup failure, and design D1b is explicit that the four tests around it are what is wrong.
- **Moving `App.Tests` off `PerTest` isolation.** D1b is the evidence that `PerTest` isolates less than `CLAUDE.md` claims. Changing a whole assembly's isolation mode to fix four tests is the larger change and needs its own argument.
