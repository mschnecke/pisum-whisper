## 1. Project setup

- [x] 1.1 Add `SoundFlow`, `Concentus` and `Concentus.Oggfile` as `PackageReference`s (no `Version` — already pinned in `Directory.Packages.props`) to `src/Pisum.Whisper.Core/Pisum.Whisper.Core.csproj`, and create the `Core/Audio/` folder (`Pisum.Whisper.Core.Audio` namespace). Verify: `dotnet build Pisum.Whisper.slnx` stays at 0 warnings.

## 2. Capture

- [x] 2.1 Add `AudioException` in `Core/Audio/AudioException.cs`: `public sealed class AudioException(string message, Exception? innerException = null) : Exception(message, innerException)`, mirroring `Settings/SettingsException.cs`'s one-type-per-capability shape. Verify: builds; no dedicated test, matching `SettingsException`, which also has none.
- [x] 2.2 Add `IAudioCapture` in `Core/Audio/IAudioCapture.cs`: `void Start()` and `Task<float[]> StopAsync()`, documented as returning every sample captured since `Start` regardless of duration, with no minimum-duration or empty-recording check. Verify: builds; fakeable via `A.Fake<IAudioCapture>()`.
- [x] 2.3 Implement `MiniAudioCapture : IAudioCapture` in `Core/Audio/MiniAudioCapture.cs`, following `spikes/Pisum.Whisper.Spikes/AudioSpike.cs`'s shape: `Start()` creates a `MiniAudioEngine`, resolves the default capture device, and calls `InitializeCaptureDevice` with `AudioFormat { SampleRate = 48_000, Channels = 1, Format = SampleFormat.F32 }`; throw `AudioException("No input device found")` if `CaptureDevices` is empty. Verify: manual — run `dotnet run --project spikes/Pisum.Whisper.Spikes -- audio` and confirm S2's PASS verdict still holds against this exact request shape (no automated test; no microphone in CI, per `design.md`).
- [x] 2.4 In the device's `OnAudioProcessed` callback, copy each buffer (`buffer.ToArray()`) and `TryWrite` it into a private unbounded `Channel<float[]>` — no lock and no blocking call on the callback thread. Verify: same manual `audio` spike run as 2.3; no dropped-buffer warnings and sample-count ratio stays within the spike's existing 97–103% tolerance.
- [x] 2.5 Implement the channel drain and `StopAsync`: extract the drain/concatenate step into an internal method taking a `ChannelReader<float[]>` so it is testable without hardware; `StopAsync` stops the device, disposes the engine/device, completes the channel writer, awaits the drain, and returns the concatenated `float[]`. Verify: unit test feeds an in-memory channel known chunks and asserts the concatenated output, plus a case where the channel completes with zero chunks written (immediate stop) returns an empty array.
- [x] 2.6 Add `AddAudioPipeline(this IServiceCollection services)` in `Core/Audio/ServiceCollectionExtensions.cs` registering `IAudioCapture -> MiniAudioCapture`, following `AddFileLogging`'s registration pattern, and call it from `Program.cs`. Verify: `dotnet build` succeeds; a debug run's `ValidateOnBuild` does not fail on the new registration.

## 3. Encoding

- [x] 3.1 Add `WavWriter` in `Core/Audio/WavWriter.cs` with `byte[] Write(float[] samples, int sampleRate)`, hand-writing a 16-bit PCM mono RIFF header (matching the reference `hound::WavSpec` shape). Verify: unit test writes a known sample array, re-parses the RIFF/fmt/data header fields, and asserts channel count 1, bit depth 16, and sample rate match the input.
- [x] 3.2 Add `OggOpusWriter` in `Core/Audio/OggOpusWriter.cs` with `byte[] Write(float[] samples, int sampleRate)`, following `spikes/Pisum.Whisper.Spikes/OpusSpike.cs`'s shape: `OpusCodecFactory.CreateEncoder(sampleRate, 1, OpusApplication.OPUS_APPLICATION_VOIP)` with `Bitrate = 24_000`, then `new OpusOggWriteStream(encoder, stream, new OpusTags(), sampleRate, 5, false)` against a `MemoryStream`, `WriteSamples` then `Finish`. Verify: unit test encodes a known-duration sample array, decodes it back with `OpusOggReadStream` (as `OpusSpike.cs` does), and asserts decoded duration is within the same 97–103% tolerance S4 used.
- [x] 3.3 Add `EncodedAudio` in `Core/Audio/EncodedAudio.cs`: `public readonly record struct EncodedAudio(byte[] Bytes, string MimeType, AudioFormat ActualFormat)`, with `"audio/ogg"` and `"audio/wav"` as the two MIME values (matching the reference's `opus_mime_type()`/`wav_mime_type()`). Verify: builds; consumed by task 3.4.
- [x] 3.4 Add `IAudioEncoder`/`AudioEncoder` in `Core/Audio/`: `EncodedAudio Encode(float[] samples, int sampleRate, AudioFormat preferred)` tries the preferred format's writer; on any exception, logs a warning (format attempted and exception type only — never sample data) and tries the other writer; propagates if that also throws `AudioException`. Verify: unit tests injecting real `WavWriter`/`OggOpusWriter` instances assert (a) preferred-format success returns that format untouched, (b) a thrown preferred writer falls back and `ActualFormat` reports the fallback, (c) both writers throwing raises an `AudioException`.
- [x] 3.5 Register `IAudioEncoder -> AudioEncoder` in `AddAudioPipeline`. Verify: same build/`ValidateOnBuild` check as 2.6.

## 4. Manual verification

- [x] 4.1 Add an `[Ignore("Requires a real microphone; run manually")]` MSTest test in `tests/Pisum.Whisper.Core.Tests/Audio/` that starts `MiniAudioCapture`, records for a few seconds, stops it, encodes the result with both `OggOpusWriter` and `WavWriter`, and writes both files to the temp directory for playback — the spikes never reference `Core`, so this is the only thing that exercises the production types end-to-end against real hardware. Verify: a developer runs it locally (`dotnet test --filter Audio.Manual`, or via IDE) on both Windows and macOS before this change ships, and confirms both files play back correctly.
- [x] 4.2 `dotnet build Pisum.Whisper.slnx` and `dotnet test Pisum.Whisper.slnx` both clean. Verify: 0 warnings; all new unit tests (2.5, 3.1, 3.2, 3.4) pass; 4.1 is skipped, not failed.
