## 1. Record the baseline

- [ ] 1.1 Capture the pre-migration numbers so the migration has something to be checked against: run `dotnet test Pisum.Whisper.slnx` and record per project the passed / skipped / total counts and the duration. Verify: the run is green and reports 366 passed + 2 skipped = 368 for `Pisum.Whisper.Core.Tests` and 2 passed + 2 skipped = 4 for `Pisum.Whisper.Platform.Tests`. If either number differs from design.md's Context table, stop and reconcile before touching anything.
- [ ] 1.2 Record the per-attribute counts the migration will be audited against by running the counting greps over `tests/` excluding `obj/`. Verify: `[TestClass]` 53, `[TestMethod]` 350, `[DataRow]` 28, `[TestInitialize]` 14, `[TestCleanup]` 14, `[Ignore]` 4.

## 2. Packages and platform

- [ ] 2.1 In `Directory.Packages.props`, remove the `MSTest` and `Microsoft.NET.Test.Sdk` entries from the `Tests` group and add `<PackageVersion Include="xunit.v3" Version="4.0.0" />`. Leave `FakeItEasy`, `Shouldly` and `SharpHook.Testing` alone. Verify: the `Tests` group holds exactly those four entries and no `PackageReference` anywhere carries a `Version` (central package management, per `CLAUDE.md`).
- [ ] 2.2 In both `tests/Pisum.Whisper.*.Tests/*.csproj`, replace the `Microsoft.NET.Test.Sdk` and `MSTest` references with a single `<PackageReference Include="xunit.v3"/>`. Keep `OutputType=Exe`, `IsPackable=false` and every other reference as they are. Verify: `dotnet restore Pisum.Whisper.slnx` succeeds and `tests/*/obj/project.assets.json` resolves `xunit.v3.core.mtp-v2`, `xunit.v3.assert` and `xunit.analyzers` transitively (design.md D1).
- [ ] 2.3 Add the MTP opt-in to `global.json` as a `test` block alongside the existing `sdk` block (design.md D2). Verify: `dotnet test --help` no longer prints the VSTest description line, and instead shows the Microsoft.Testing.Platform options.
- [ ] 2.4 Add `tests/Pisum.Whisper.Core.Tests/GlobalUsings.cs` and `tests/Pisum.Whisper.Platform.Tests/GlobalUsings.cs`, each containing `global using Xunit;` (design.md D8). Verify: the files exist; the solution does not build yet, which is expected until section 3 completes.

## 3. Migrate the test source

Each task in this section is: delete `[TestClass]`, turn `[TestMethod]` into `[Fact]` or `[Theory]`, turn `[DataRow]` into `[InlineData]`, turn `[TestInitialize]` into a constructor and `[TestCleanup]` into `IDisposable.Dispose()`, per the mapping table in design.md D3. No assertion, arrangement or test name changes.

- [ ] 3.1 `tests/Pisum.Whisper.Platform.Tests` — 3 files, 3 classes, 4 tests, no lifecycle methods, 2 `[Ignore]` methods becoming `[Fact(Skip = …)]`. Verify: `dotnet test tests/Pisum.Whisper.Platform.Tests` reports 2 passed, 2 skipped, 4 total. This is the end-to-end proof that the packages, the runner and the mapping all work, so do not start 3.2 until it is green.
- [ ] 3.2 `Core.Tests/Settings` (3 files, 3 classes, 32 tests) and `Core.Tests/TestHarnessTests.cs` (1 class, 1 test). Two `[TestInitialize]`/`[TestCleanup]` pairs become constructors and `Dispose()`. Verify: `dotnet test tests/Pisum.Whisper.Core.Tests` compiles these files and the 33 tests pass; the rest of the project is still broken, which is expected.
- [ ] 3.3 `Core.Tests/Logging` — 10 files, 8 classes, 35 tests, 15 `[DataRow]` rows across 3 methods, and 4 lifecycle pairs including the `FileLoggingTestBase` abstract base. Verify: 35 tests pass, and the three `LogLevelNamesTests` theories still report 15 rows in total, with `[InlineData(null)]` bound to the `string? name` parameter.
- [ ] 3.4 `Core.Tests/Audio` — 5 files, 5 classes, 11 tests. `ManualCaptureSmokeTest` carries the class-level `[Ignore]`; move the reason onto `Skip` on its single `[Fact]` and leave the explanatory XML doc on the class (design.md D5). Verify: 10 passed, 1 skipped, and the skip reason still reads "Requires a real microphone; run manually" in the runner output.
- [ ] 3.5 `Core.Tests/Transcription` — 7 files, 6 classes, 54 tests, 8 `[DataRow]` rows, 2 lifecycle pairs. The 4 rows in `GeminiWireTests.cs` carry `DisplayName` and must become `TestDisplayName` (design.md D3). Verify: 53 passed, 1 skipped, and `no candidate`, `no part`, `no text` and `nothing at all` are still the displayed names of those four rows.
- [ ] 3.6 `Core.Tests/Output` — 8 files, 6 classes, 31 tests, 2 lifecycle pairs including the `TextOutputTestBase` abstract base. `TextOutputTestDoubles.cs` holds no attributes and should come out unchanged. Verify: 31 tests pass.
- [ ] 3.7 `Core.Tests/Hotkeys` — 15 files, 12 classes, 114 tests, 2 lifecycle pairs including the `GlobalHotkeyServiceTestBase` abstract base. `BlockingHookProvider.cs` and `RecordingLogSource.cs` hold no attributes and should come out unchanged. Verify: 114 tests pass.
- [ ] 3.8 `Core.Tests/Dictation` — 9 files, 9 classes, 68 tests, 5 `[DataRow]` rows, 2 lifecycle pairs including the `DictationTestBase` abstract base. Note that `DictationLifecycleTests.cs` holds two classes and only the standalone `DictationRegistrationTests` owns a lifecycle pair (design.md D4). Verify: 68 tests pass.
- [ ] 3.9 Audit the translation by counting rather than by reading: no `[TestClass]`, `[TestMethod]`, `[DataRow]`, `[TestInitialize]`, `[TestCleanup]` or `[Ignore]` remains anywhere under `tests/`, and the new counts are `[Fact]` 344, `[Theory]` 6, `[InlineData]` 28, `IDisposable` implementations 14. Verify: the greps return those numbers exactly; a shortfall in `IDisposable` means a lost `[TestCleanup]`, which under parallel execution leaks a fixture rather than failing loudly.

## 4. Warnings-as-errors

- [ ] 4.1 Build the solution and collect the full `xunit.analyzers` diagnostic list in one pass: `dotnet build Pisum.Whisper.slnx`. Verify: the list is written down before any of it is fixed, so section 4.2's scope is known rather than discovered one file at a time.
- [ ] 4.2 Fix the two xUnit1031 blocking waits by making their test methods `async Task` and awaiting: `Logging/FileLoggingBufferTests.cs:79` and `Logging/FileLoggingRegistrationTests.cs:97`, both `host.StopAsync(…).GetAwaiter().GetResult()`. Verify: both files build clean and their tests still pass.
- [ ] 4.3 Resolve the xUnit1051 diagnostics by passing `TestContext.Current.CancellationToken` to the flagged `Task.Delay` and other cancellable calls (design.md D7). Verify: no xUnit1051 remains and the affected tests still pass; the token must not be threaded into `Hotkeys/BlockingHookProvider.cs`, whose block is deliberate.
- [ ] 4.4 Resolve any remaining diagnostic — including anything reaching `BlockingHookProvider.cs:35` — by fixing the code, or by a scoped `#pragma warning disable` with the reason written beside it. Verify: `dotnet build Pisum.Whisper.slnx` reports 0 warnings and 0 errors, and `Directory.Build.props` and both `.csproj` files contain no `NoWarn` (design.md D7).

## 5. Verify the migration

- [ ] 5.1 Confirm the suite is whole: `dotnet test Pisum.Whisper.slnx`. Verify: 372 tests total, 368 passed, 4 skipped — the same numbers as task 1.1, split the same way between the two projects.
- [ ] 5.2 Confirm the parallelism decision holds (design.md D6) by running `dotnet test Pisum.Whisper.slnx` five times in a row. Verify: five consecutive green runs. Any test that passes alone but fails in the suite is a shared-state finding that the D6 audit missed — record which one, and resolve it by making that fixture independent rather than by disabling parallelism.
- [ ] 5.3 Establish and record the MTP incantation for running one manual test by name, replacing the VSTest `--filter` syntax the archived tasks quote. Verify: the command actually runs `ManualClipboardRoundTrip` alone on this Windows machine, and its clipboard round trip passes.
- [ ] 5.4 Confirm Rider still discovers and runs the tests (design.md Risks). Verify: the Unit Tests window lists all 372 tests and a single test runs from the gutter. If discovery fails, stop and take the documented VSTest-bridge fallback rather than working around it.

## 6. Documentation

- [ ] 6.1 Update `README.md:110` — the table row naming `MSTest, FakeItEasy, Shouldly` for `tests/Pisum.Whisper.Core.Tests` — and add the row for `tests/Pisum.Whisper.Platform.Tests` if it is still missing. Verify: no occurrence of "MSTest" remains in `README.md`.
- [ ] 6.2 Update the `context` block in `openspec/config.yaml:42` from "MSTest + FakeItEasy + Shouldly for tests" to name xUnit v3, so future proposals are generated against the real stack. Verify: `openspec instructions proposal --change <any-open-change> --json` shows the corrected context.
- [ ] 6.3 Add a short test-stack section to `CLAUDE.md` covering what the next person cannot infer from the code: that `dotnet test` runs on Microsoft.Testing.Platform because of `global.json`, that VSTest `--filter` syntax no longer works, the verified command from task 5.3 for running one manual test by name, and that tests run in parallel by class with no shared mutable state to protect. Verify: a reader following only `CLAUDE.md` can run the full suite and one manual test by name.
- [ ] 6.4 Add this change to the `openspec/ROADMAP.md` artifact status or standing-decisions section in whichever form that file already uses for work outside the 12 numbered changes. Verify: `openspec validate migrate-tests-to-xunit-v3` passes and the roadmap does not claim the test stack is MSTest.
