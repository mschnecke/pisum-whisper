## 1. The notification service

- [ ] 1.1 Add `Core/Notifications/`: `INotificationService` with `Notify(string title, string message)` and `NotifyInformation(string title, string message)`, `INotificationPresenter` with `Present(string title, string message)`, an `internal sealed NotificationService` implementing the first over the second, and `AddNotifications()` registering it as a singleton. `NotificationService` reads `SettingsStore.Current.ShowTrayNotifications` **per call** — no `Changed` subscription and nothing to rebuild, matching `GeminiProviderPool` and `DictationOrchestrator`. Two named methods rather than one with a `force` flag: the reference found that shape (`tray.rs:76-105`) and a boolean at six call sites reads worse. Verify: unit tests in `Core.Tests/Notifications` over a fake presenter — a forced notification presents with the preference off, an informational one does not, both present with it on, and flipping the preference between two calls changes the second without re-registering anything. `Integration` trait: holding a non-default flag needs a real `SettingsStore` over a temp file.

## 2. The toast

Everything here is `src/Pisum.Whisper.App/Notifications/`. There is deliberately **no type in
`Core`** for the presentation, following changes 9 and 10 — the tray icon and the settings window are
both App-only — and `design.md` records why the seam sits at `INotificationPresenter` instead.

- [ ] 2.1 Add `ToastWindow.axaml` and its code-behind, following the settings window's axaml-plus-code-behind layout rather than the spike's code-only construction. `ShowActivated = false`, `Topmost = true`, `ShowInTaskbar = false`, `WindowDecorations = WindowDecorations.None`, `CanResize = false`, 360 x 96 logical. **`SystemDecorations` is obsolete in 12.1** in favour of `WindowDecorations`; using the old name is a warning and therefore a build failure. Place from `Screens.Primary`'s `WorkingArea` and `Scaling`, 16 logical from both edges: bottom-right stacking upward on Windows, **top-right stacking downward on macOS**, because that is where each platform's notifications live. `Position` is physical pixels while `Width` and `Height` are not, which is the whole reason `Scaling` is read. Fixed appearance — explicit brushes, a dark card with light text, no `FluentTheme` dependency — so the dark-theme non-goal is sidestepped rather than reopened. Verify: `[AvaloniaFact]` in `App.Tests/Notifications` asserting the window shows, that its computed position for slot 0 and slot 1 both fall inside a stubbed working area, and that the two rectangles do not intersect; `dotnet build Pisum.Whisper.slnx` at 0 warnings.
- [ ] 2.2 Add `ToastPresenter : INotificationPresenter`. `Present` posts to `Dispatcher.UIThread` and **returns immediately** — it may not wait for the window, and an implementation that does is wrong, because two call sites run on the hotkey dispatch loop where the next item may be the release edge ending a recording. Own the live list, assign slots, cap at **three** concurrent with a fourth closing the oldest, and dismiss each after 6 s with a `DispatcherTimer` whose dwell is **injected as a `TimeSpan`** so no test waits six seconds — the precedent is `SettingsEditor`'s injected debounce and `GeminiProvider`'s injected backoff. Verify: `[AvaloniaFact]`s covering the cap, the dismissal, and that `Present` returns before the window exists; plus a test that calls `Present` from a non-UI thread and completes.
- [ ] 2.3 Register `INotificationPresenter` -> `ToastPresenter` in `Program.cs` beside `SettingsEditor` and `SettingsWindowViewModel`, and call `AddNotifications()` there too. Close every open toast from `App.OnExit`, beside `_trayIcon?.Dispose()`, so one cannot outlive the dispatcher that owns it. Registering the two halves separately is the point: with `ValidateOnBuild` on, omitting the presenter is a startup failure naming `INotificationPresenter` rather than a null reference at the first error a user hits. Verify: a registration test resolving `INotificationService` and `INotificationPresenter` from a built host, in the manner of `SettingsWindowRegistrationTests`; and a manual quit with a toast on screen exits with code 0.

## 3. The six call sites

- [ ] 3.1 Give `DictationOrchestrator` an `INotificationService` constructor dependency and add one call beside each of the five existing log statements — `TryStartRecording` :358 and :381, `DictateAsync` :515, `RunAsync` :472, and the watchdog at :590. Forced (`Notify`) for the capture failure, the dictation failure and the paste failure; informational (`NotifyInformation`) for "Transcription In Progress" and "Recording Auto-Stopped". Change 8 already produces every title and message and holds them in locals, so each site is one line. **Do not touch `DictationFailure`** — it stays `internal` and unchanged, and there is no new title mapping to write. **The shutdown filter stays ahead of the notification**: `RunAsync`'s `catch (OperationCanceledException) when (_shutdown.IsCancellationRequested)` already separates "the user quit" from "the budget expired", and quitting must stay silent. Verify: unit tests in `Core.Tests/Dictation` over a fake `INotificationService` asserting each of the five fires with the expected title, that a shutdown-cancelled dictation notifies nothing, and that the two informational ones are the only ones suppressed when the preference is off. `Integration` trait — `DictationTestBase` creates a temp home.
- [ ] 3.2 Confirm by inspection that nothing on the notification path can block the hotkey: `Present` posts and returns, `NotificationService` does one field read, and no call site awaits anything. This is the rule that quietly disqualifies `osascript` — a `Process.Start` per notification on the thread that owns the user's hotkey — and it is invisible at the call site, which is why it is checked rather than assumed. Verify: by inspection, plus the existing `dictation-pipeline` scenario *A dictation is in progress / hotkey edges continue to be reported* still passing.

## 4. Autostart

- [ ] 4.1 Add `Core/Autostart/`: `IAutostartService` with `bool IsEnabled()`, `void Enable()` and `void Disable()` — methods rather than a property, because all three do I/O — and `AutostartException`, mirroring `SystemShellException`. Add `AutostartReconciler`, an `IHostedService` that reconciles in `StartAsync` and on every `SettingsStore.Changed`: read `IsEnabled()`, compare to `Current.StartWithSystem`, write **only** on a mismatch, and log only when it writes. Reading before writing is the fix for a mistake already in the tree — `GlobalHotkeyService.OnSettingsChanged` logs `"Hotkey rebound to {Binding}."` outside `Rebind`'s early return, so changing the audio format writes a misleading line at the default level, and here the write would be a registry mutation rather than a no-op. Catch `AutostartException` and log it: a machine that refuses a Run value must not stop the application starting. Add `AddAutostart()`. Verify: unit tests over a fake `IAutostartService` and a fake store — enables when the setting is on and nothing is registered, disables when off and registered, **writes nothing when they already agree**, reconciles again on `Changed`, and survives a throwing service. `Unit` trait: no file, no container.
- [ ] 4.2 Add `Platform/Autostart/WindowsAutostart`, `[SupportedOSPlatform("windows")]`, writing value `Pisum Whisper` under `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` with the quoted `Environment.ProcessPath`. **The subkey path is injected**, defaulting to the real one — that is what makes this testable at all, and without it the only honest test writes to the user's real Run key and the capability is verified by hand for ever. **`Microsoft.Win32.Registry` needs no package reference**: verified in a scratch `net10.0` project with `TreatWarningsAsErrors`, the types resolve from the shared framework and the only diagnostics are three `CA1416`s, cleared by the platform attribute plus the `OperatingSystem.IsWindows()` guard in 4.4 — the same pattern `WindowsClipboard` already uses. Do **not** add anything to `Directory.Packages.props`. Verify: round-trip tests against a private `HKCU\Software\Pisum.Whisper.Tests\...` subkey deleted in `Dispose` — enable, `IsEnabled()` true, the value names the running executable, enable again and there is still one value, disable, `IsEnabled()` false. `Integration` trait, per 6.1.
- [ ] 4.3 Add `Platform/Autostart/MacOsAutostart`, `[SupportedOSPlatform("macos")]`, writing `net.pisum.whisper.plist` into `~/Library/LaunchAgents` with `Label` `net.pisum.whisper`, `ProgramArguments` `[Environment.ProcessPath]` and `RunAtLoad` `true`, and deleting it to disable. **The directory is injected**, defaulting to the real one. No `launchctl`: `tauri-plugin-autostart`'s `MacosLauncher::LaunchAgent` writes the plist and nothing else, and `launchd` reads the directory at login. Verify: round-trip tests writing into a temp directory — these run **on any operating system**, so the plist's shape and the enable/disable/re-enable cycle are covered from Windows and only the effect on login needs hardware (7.2). Assert the written XML parses and carries all three keys.
- [ ] 4.4 Add `AddNativeAutostart()` following `NativeOutputServiceCollectionExtensions` exactly — `OperatingSystem.IsWindows()`, then `IsMacOS()`, then `throw new PlatformNotSupportedException` naming `RuntimeInformation.OSDescription` — and call `AddAutostart()` and `AddNativeAutostart()` from `Program.cs`. Verify: registration tests in `Platform.Tests` resolving `IAutostartService` to the concrete type for the running platform, in the manner of `NativeShellRegistrationTests`; `dotnet test Pisum.Whisper.slnx` green.

## 5. First launch

- [ ] 5.1 In `App.OnFrameworkInitializationCompleted`, when `SettingsStore.IsFirstLaunch`, notify the welcome (forced, matching `lib.rs:588`) and then call `ShowSettings()`. In that order: the welcome points at the window, so it must not land on top of it. The autostart half of the reference's first-launch block needs nothing here — 4.1's reconciler has already run in `StartAsync`, which is a deliberate divergence recorded in `design.md`. It goes here rather than in `Program.LoadSettings` because the flow shows a window and `Program` runs before Avalonia exists. Verify: `[AvaloniaFact]`s — a first launch shows the window and notifies once; a subsequent launch does neither; closing the window afterwards hides it and leaves the application running, which is change 10's `WindowClosing` behaviour unchanged.

## 6. Documentation and rules

- [ ] 6.1 Update `CLAUDE.md`: a *Notifications and autostart* section beside the existing capability sections carrying (a) that the notification is a window this application draws, and why — the toast package needs a `-windows` TFM the project has decided against, and `Shell_NotifyIcon` needs a second tray icon and reaches no notification platform; (b) that `INotificationPresenter.Present` **must not block**, because two call sites run on the hotkey dispatch loop; (c) that autostart is reconciled rather than toggled, and reads before it writes; (d) a **sixth logging rule** — never put a transcript, an API key or clipboard contents in a notification, which is the same rule as the five existing ones applied to a surface that is drawn over whatever the user is presenting; and (e) one word added to the test-category rule, so that "or writes to the real registry" makes `WindowsAutostartTests` `Integration`. Verify: all five are written down, and `git show --stat` confirms the `CLAUDE.md` change lands with the code it describes rather than after it.
- [ ] 6.2 Correct `openspec/ROADMAP.md`'s *Artifact status*, which is stale: it says change 8 is not archived and that changes 10-12 have `proposal.md` only, but `2026-09-01-add-dictation-pipeline` and `2026-09-01-add-settings-window` are both in `archive/` and only 11 and 12 are proposal-only. Verify: the section describes the tree as it stands; `ls openspec/changes/archive/` agrees with it.

## 7. Verification

- [ ] 7.1 Verify the whole capability on win-x64 by hand. Delete `~/.pisum-whisper.json`, launch, and confirm the welcome notification appears and the settings window opens itself. Then: turn tray notifications off and confirm a forced failure still appears while pressing the hotkey mid-transcription shows nothing; hold the hotkey with no microphone attached and confirm the recording failure is shown; confirm focus stays in the application you were typing in while a notification is up, and that a dictation delivered immediately afterwards still pastes there. Check `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` gains and loses `Pisum Whisper` as the General tab's switch is toggled. Verify: manual; record the results in `design.md` under a *Verification results* heading in the manner of change 7. Note the registered path — under `dotnet run` it will be the build output, which is expected and recorded in *Risks*.
- [ ] 7.2 Verify the capability on osx-arm64 by hand, as 7.1 does on Windows, plus the two things only macOS can answer: that a toast appears at the **top right** below the menu bar and does not activate the application — Avalonia's `orderFront:` path says it will not, but that is reading rather than running — and that `~/Library/LaunchAgents/net.pisum.whisper.plist` appears, and that logging out and back in starts the application. Verify: manual on Apple Silicon with Accessibility granted beforehand; record beside 7.1. Needs the same hardware as 7.3 and as changes 8, 9 and 10's outstanding macOS tasks, and should be done in one sitting with them.
- [ ] 7.3 Run `spikes -- toast` on macOS. It is written to run there unchanged and falls back to three y/n prompts, because `WindowFromPoint` has no cheap equivalent through this codebase. This is what closes the *nothing here has run on macOS* risk in `design.md` for the transport itself, separately from 7.2's check of the shipped window. Verify: manual on Apple Silicon; record the answers in `design.md` beside the win-x64 measurements.
- [ ] 7.4 Answer the two open questions that need running rather than reasoning. **The first is not "is the toast late".** Call `Present` before Avalonia is initialised **from a pooled thread** — `Task.Run`, or better, a real capture failure driven from a hotkey press between `host.Start()` and `StartWithClassicDesktopLifetime` — and then confirm the application still comes up with a working dispatcher and that later notifications render. Posting from the main thread instead tests the case Avalonia 12 explicitly supports (`#18586`, `#18686`) and returns a false pass, so it is the wrong experiment. What is being looked for is in `design.md`: `Dispatcher`'s `_thread` is readonly, `s_uiThread ??= this` makes the first dispatcher constructed anywhere the process's UI thread for good, and a missing platform implementation falls back to `ManagedDispatcherImpl` **silently**. A pass means the toast appears late and nothing else breaks; a fail means `ToastPresenter` must drop notifications until `App` signals readiness. The second question is whether `ShowInTaskbar = false` also keeps the toast out of the alt-tab list on Windows, which needs `WS_EX_TOOLWINDOW` and was not measured. Verify: the first by a headless test posting from a pooled thread plus the deliberate early call, asserting the process still starts and a later notification renders; the second by a glance during 7.1. Record both in `design.md`'s *Open Questions*, striking them through in the manner of change 9.

## Not in this change

Recorded so they are not mistaken for oversights, each with its owner:

- **`IShellService`.** The proposal asks for it; change 10 already shipped it as `Core/Shell/ISystemShell` and `Platform/Shell/SystemShell`, wired into the Logging tab. Nothing to do, and `design.md`'s drift table records it.
- **Respecting Do Not Disturb and Focus Assist.** A window this application draws is not suppressed by either, and nothing here mitigates it. It is the accepted cost of not depending on change 12, and it is in `design.md`'s *Risks* rather than left to be discovered.
- **Notification history.** Nothing this change shows persists anywhere after it is dismissed. A native transport would give it; S6 established that `Shell_NotifyIcon` would not have, so this is not a regression against the option that was rejected.
- **A native transport behind `INotificationPresenter`.** The interface exists so one can be added once change 12 registers an AUMID, and `spikes -- notify` stays in the tree because that is when its three unanswered observational questions matter again.
- **Rendering `HotkeyAvailability.Failed`.** Change 9 deferred the dead-hotkey case here on the grounds that this change brings a notification transport. It is genuinely adjacent, but it is a startup condition rather than a dictation failure, it has no title or message written for it, and the macOS grant it reports on is unverified on hardware — so it would widen this change past its proposal on top of behaviour nobody has observed. It needs its own proposal.
- **Packaging, the Start-menu shortcut and the AUMID.** Change 12. This change is written so that none of them are prerequisites.
