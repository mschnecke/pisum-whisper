# fix-stale-autostart-registration

A login registration naming an executable that is no longer this one was read as agreement, so an install over a build went on launching the old path at every login
